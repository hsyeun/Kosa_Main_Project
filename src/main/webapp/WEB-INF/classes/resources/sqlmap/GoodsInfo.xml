<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GoodsInfo">
	<select id="getGoodsInfoList" resultType="app.goodsinfo.GoodsInfo">
		<![CDATA[
		  select code, name, price, maker from goodsinfo
		]]>
	</select>
	
	<select id="getGoodsInfoContent" parameterType="string" resultType="app.goodsinfo.GoodsInfo">
		<![CDATA[
		  select code, name, price, maker from goodsinfo where code=#{code}
		]]>
	</select>
	
	<insert id="insertGoodsInfo" parameterType="app.goodsinfo.GoodsInfo">
		<![CDATA[
		  insert into goodsinfo (code, name, price, maker) values (
		  	#{code}, #{name}, #{price}, #{maker}
		  )
		]]>
	</insert>
	
	<update id="updateGoodsInfo" parameterType="app.goodsinfo.GoodsInfo">
		<![CDATA[
		  update goodsinfo set name=#{name}, price=#{price}, maker=#{maker}
		   where code=#{code}
		]]>
	</update>	

	<delete id="deleteGoodsInfo" parameterType="string">
		<![CDATA[
		  delete from goodsinfo where code=#{code}
		]]>
	</delete>	
</mapper>

